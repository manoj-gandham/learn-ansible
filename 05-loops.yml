- name: demo for loops
  hosts: localhost
  connection: local
  tasks:
    - name: loops demo
      ansible.builtin.debug:
        msg: city name - {{ city }}
      loop:
        - hyd
        - banglr